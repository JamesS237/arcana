<%= form_for(@type) do |f| %>
  <% if @type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
      <% @type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
    <fieldset>
	<%= f.text_field :name, :placeholder => "Type" %><br><br>
	<%= f.number_field :weight, :placeholder => "Weight" %><br><br>

 	 <%= f.submit "Add Type", class: "btn" %>
  </fieldset>
<% end %>
